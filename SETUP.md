-- This script helps initialize the database with proper schema
-- Run this if you're seeing API 500 errors

-- The database should be automatically created when you run:
-- npx prisma db push

-- This will:
-- 1. Create all tables defined in prisma/schema.prisma
-- 2. Set up relationships and indexes
-- 3. Apply any schema changes

-- After running the command, restart your dev server
